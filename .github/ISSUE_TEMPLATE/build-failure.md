---
name: Build failure
about: The local build script failed
labels: build
---

**macOS version:**

**Xcode version** (`clang --version`):

**Chip** (e.g. M1, M2 Pro, M4 Max):

**MKVToolNix version / tag:**

**Command run:**
```
./build-local.sh release-XX.X
```

**Which step failed** (dependency name or mkvtoolnix):

**Error output** (last ~50 lines):
```

```

**Did a previous build succeed?** (yes/no, which version):
