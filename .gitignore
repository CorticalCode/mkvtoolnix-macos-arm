# Multi-agent review scratch system (Claude, Codex, Gemini)
/

# AI agent configuration (private, backed up to ~// project area)


# Claude Code project config (local permissions, memory)
/

# Distribution outputs (DMGs)
dist/

# Per-architecture build counters (local state)
.build-counter-*
# macOS
.DS_Store
**/.DS_Store
._*
.AppleDouble
.LSOverride
.Spotlight-V100
.Trashes

# Windows
Thumbs.db
ehthumbs.db
Desktop.ini
$RECYCLE.BIN/

# Linux
*~
.directory

# Editors and IDEs
.vscode/
.idea/
*.swp
*.swo
